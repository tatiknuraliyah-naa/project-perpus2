@extends('layouts.app')
@section('content')<main class="app-page"><header class="page-header"><div><p class="eyebrow">Informasi</p><h1>Pengumuman</h1></div>@if(auth('petugas')->check())<a class="button" href="{{route('pengumuman.create')}}">Tambah</a>@endif</header><section class="content-card">@forelse($pengumuman as $item)<article><h2>{{ $item->judul }}</h2><p class="muted">{{ $item->tanggal_publish->format('d M Y') }} · {{ $item->petugas->nama }}</p><p>{!! nl2br(e($item->isi)) !!}</p>@if(auth('petugas')->check())<a class="text-link" href="{{route('pengumuman.edit',$item)}}">Ubah</a><form style="display:inline" method="POST" action="{{route('pengumuman.destroy',$item)}}">@csrf @method('DELETE')<button class="link-button">Hapus</button></form>@endif<hr>@empty<p class="empty-state">Belum ada pengumuman.</p>@endforelse{{ $pengumuman->links() }}</section></main>@endsection
